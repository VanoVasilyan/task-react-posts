import React, { FC } from 'react';
import * as SC from './styles';

const Loading: FC = () => (
    <SC.StyledCircleMainBlock>
        <SC.StyledCircleContainer />
    </SC.StyledCircleMainBlock>
);

export default Loading
